<h2>Edit Comment</h2>

<div class="form">
<%= form_for [@post, @comment] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.hidden_field :author, value: current_user.email %>

  <%= f.submit "Edit Comment!"%>
<% end %>
</div>
